import { PreviewProps } from "./Preview"

const RankImage: React.FC<PreviewProps> = ({
  imgsrc,
  like,
  shopName,
  modelName,
  productName,
  price,
  colors
}: PreviewProps) => {
  return (
    <>
    {/* 상품 랭킹 이미지 */}
    </>
  )
}

export default RankImage